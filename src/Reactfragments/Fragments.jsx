import React, { Fragment } from "react";

const Fragments = () => {
  // ** fragments are nothing but tags which does not create extra node
  return <Fragment>Fragments</Fragment>;
};

export default Fragments;
